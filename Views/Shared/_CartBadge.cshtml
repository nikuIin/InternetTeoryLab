<!-- Views/Shared/_CartBadge.cshtml -->
@{
    // Этот метод будет вызывать CartController, который мы создадим позже.
    // Пока что оставим заглушку, которую будет заполнять JavaScript.
}
<span id="cart-badge" class="cart-badge" style="display: none;">0</span>

<style>
    .cart-badge {
        position: absolute;
        top: -8px;
        right: -8px;
        background-color: var(--primary-color);
        color: white;
        border-radius: 50%;
        padding: 2px 6px;
        font-size: 0.7rem;
        font-weight: bold;
        min-width: 18px;
        text-align: center;
    }
    .header__cart-icon {
        position: relative;
    }
</style>
